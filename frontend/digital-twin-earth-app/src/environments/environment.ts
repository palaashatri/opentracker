export const environment = {
  production: false,
  apiBaseUrl: '',  // proxy handles /api/** → localhost:8080
  cesiumAccessToken: '',  // empty = open source mode (Bing maps unavailable, OSM works)
};
